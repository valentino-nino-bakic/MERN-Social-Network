Utilizing 'multer' package, the 'uploads' directory is created
dynamically when user uploads image from his local machine.

Although storing uploads like this is a good approach when we don't
expect large amount of users in our app,
the better approach would be to use some cloud storage or cdn...
